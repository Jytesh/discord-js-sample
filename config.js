//This is a js file, since comments are not allowed in JSON files, you can change it to JSON by altering code

module.exports = {
    prefix : '!',
    /**
     * @param  {string} : One of [ERROR,DEBUG,INFO]
     */
    LOG_LEVEL : 'DEBUG', 
    token : '' //Fil token here
}